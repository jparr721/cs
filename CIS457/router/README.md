# Router
Custom routing system in C++

### Requirements
- clang++
- cmake

### Compilation
- Move into `$PROJECT_ROOT/build`
- Execute `cmake ..`

### Execution
- Execute the binary in `$PROJECT_ROOT/bin/router`
