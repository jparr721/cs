# -*- python -*-

main = executable(
    'bae',
    files=find_files('bayes', '*.cc', extra='*.h'),
    compile_options=(
        ['-std=c++1z', '-Wall', '-Werror', '-Wextra', '-pedantic', '-fopenmp', '-libstdc++fs']),
    link_options=['-fopenmp'])
